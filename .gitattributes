# JSONL hashes are part of the frozen data lineage; never rewrite line endings.
*.jsonl -text

# GitHub rejects regular Git blobs above 100 MB.
data/processed/train.jsonl filter=lfs diff=lfs merge=lfs -text
